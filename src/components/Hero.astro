<section class="hero">
  <div class="hero__media">
    <video
      class="hero__video"
      autoplay
      muted
      loop
      playsinline
      preload="metadata"
    >
      <source src="/videos/hero.mp4" type="video/mp4" />
    </video>
    <div class="hero__overlay"></div>
  </div>

  <div class="container hero__content">
    <div class="hero__branding">
      <img
        class="hero__logo"
        src="/images/MacOda.png"
        alt="MaCoda art and design logo"
      />
    </div>

<p class="hero__copy">
  Exploring symbolic imagery, storytelling, and visual identity through art and design.
</p>

    <div class="button-row">
      <a class="button button--primary" href="#selected-work">View Work</a>
      <a class="button button--secondary" href="#about">About the Artist</a>
    </div>

    <a class="hero__scroll" href="#selected-work">Scroll to Explore</a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: grid;
    align-items: center;
  }

  .hero__branding {
    display: flex;
    justify-content: center;
    margin-bottom: 1.5rem;
  }


  .hero__logo {
    width: min(680px, 92vw);
    height: auto;
    object-fit: contain;
    filter: drop-shadow(0 8px 24px rgba(0, 0, 0, 0.35));
  }

  .hero__copy {
    max-width: 640px;
    margin: 0 auto 2rem;
    font-size: 1.05rem;
    color: var(--text-dim);
  }

  .hero__media,
  .hero__overlay {
    position: absolute;
    inset: 0;
  }

  .hero__video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero__overlay {
    background:
      linear-gradient(to bottom, rgba(8, 6, 6, 0.062), rgba(8, 6, 6, 0.205)),
      radial-gradient(circle at center, rgba(0, 0, 0, 0.021), rgba(0, 0, 0, 0.315));
  }

  .hero__content {
    position: relative;
    z-index: 1;
    max-width: 760px;
    text-align: center;
    padding-top: 6rem;
    padding-bottom: 4rem;
  }

  .hero__eyebrow {
    margin: 0 0 1rem;
    color: var(--text-dim);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 0.9rem;
  }

  .hero__title {
    margin: 0;
    font-size: clamp(3.8rem, 10vw, 7rem);
    line-height: 0.95;
    font-weight: 500;
  }

  .hero__copy {
    max-width: 640px;
    margin: 1.5rem auto 2rem;
    font-size: 1.05rem;
    color: var(--text-dim);
  }

  .button-row {
    justify-content: center;
  }

  .hero__scroll {
    display: inline-block;
    margin-top: 3rem;
    color: var(--text-dim);
    font-size: 0.95rem;
  }
</style>